# AFR public eval seeds

This directory contains small public eval records for Agent Flight Recorder behavior.

These records are intentionally compact. They check whether AFR preserves observable run-boundary evidence and avoids unsupported claims. They do not contain private user data, production traces, model hidden reasoning, or chain-of-thought.

See [docs/evals.md](../docs/evals.md) for the schema and promotion rules.
